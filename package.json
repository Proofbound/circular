{
  "name": "circular",
  "version": "1.0.0",
  "description": "Karam & Sprague's Fortnightly Circular — static magazine",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "sync": "node scripts/sync-gdrive.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "googleapis": "^171.4.0"
  }
}
